/* eslint-disable @next/next/no-img-element */
'use client';

import { InstitutionList } from '@/app/(main)/pages/institution/InstitutionList';

const Institution = () => {
    return (
        <InstitutionList />
    );
};

export default Institution;
