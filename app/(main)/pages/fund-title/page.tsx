/* eslint-disable @next/next/no-img-element */
'use client';
import { FundTitleList } from '@/app/(main)/pages/fund-title/FundTitleList';

const FundTitle = () => {
    return (
        <FundTitleList />
    );
};

export default FundTitle;
