/* eslint-disable @next/next/no-img-element */
'use client';
import { SectionList } from '@/app/(main)/pages/section/SectionList';

const Section = () => {
    return (
        <SectionList />
    );
};

export default Section;
